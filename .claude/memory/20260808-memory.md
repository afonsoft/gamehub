# 2026-08-08 — long-term memory

## Decisions
- **PR #128 é o merge inicial sugerido** para corrigir o `package-lock.json` desatualizado do `angular-admin` (introduzido pelo PR #126, que bumpou `@angular/compiler` no `package.json` para 20.3.27 mas não no lock). O PR #128 atualiza o lock para 20.3.27 e ainda bumpa o `dompurify` de 3.4.12 para 3.4.13.
- **PR #113 fica parcialmente supérfluo** em relação ao #128 (ambos atualizam o `@angular/compiler` no lock), mas pode ser rebased se #128 não for mergeado.
- **PRs #125/#124/#123/#122/#121/#127 precisam de rebase/re-run** dos checks após o merge do #128, pois seus checks passaram quando o `package-lock.json` ainda estava consistente com o `package.json` (antes do #126).
- **PR #14 rejeitado/fechado**: sobe `@angular/pwa` para 22.1.3 e `@angular/cli` para 21.0.4, major versions incompatíveis com o Angular 20.3.x do gamehub/EAF 9.4.4.
- **PR #3 rejeitado/fechado**: altera `Api/src/Eaf.ProjectName.Core/Eaf.ProjectName.Core.csproj`, caminho inexistente no gamehub, e possui conflitos.
- **README.md e README.pt-BR.md** receberam badges de stack (.NET 10, Angular 20, EAF 9.4.4, Node.js 20+, PostgreSQL 16+) na branch `devin/readme-badges`.

## Lessons learned
- `gh pr merge` para `main`/`master` é bloqueado neste ambiente (política do `gh`/CI); o usuário precisa clicar em merge. Posso comentar, fechar e aprovar via PR comments, mas não executar o merge.
- O `package-lock.json` do `angular-admin` ficou desatualizado após bump manual do `package.json` no PR #126; `npm ci` falha com `lock file's @angular/compiler@20.3.26 does not satisfy @angular/compiler@20.3.27`.

## Checkpoints
- `eb5bfbb` — README badges branch pushed.
