namespace GameHub.Catalog
{
    /// <summary>
    /// Indicates the primary input scheme supported by a game.
    /// </summary>
    public enum GameControlScheme
    {
        Keyboard = 0,
        Touch = 1,
        Both = 2
    }
}
